## Packages
framer-motion | Page transitions and scroll-triggered animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility function to efficiently merge Tailwind CSS classes
lucide-react | Beautiful, consistent icons

## Notes
- Contact form submits to POST /api/contact
- Theme toggle relies on a local React context or next-themes (custom implementation provided for full control without Next.js specifics)
- The portfolio is designed as a single-page scrolling experience, utilizing anchor links for navigation.
